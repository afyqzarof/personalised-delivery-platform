# Base URL of the KatKin backend API.
# Defaults to http://localhost:3000 when unset.
VITE_API_BASE_URL=http://localhost:3000

# The client serves a stubbed response by default while the backend is built.
# Set to false to hit the real API instead.
VITE_USE_MOCK_API=true
