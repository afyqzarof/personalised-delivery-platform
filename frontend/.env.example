# Base URL of the backend API.
# Defaults to http://localhost:3000 when unset.
VITE_API_BASE_URL=http://localhost:3000

# The client hits the real API by default. Set to true to serve a stubbed
# response instead (e.g. for offline UI work without the backend running).
VITE_USE_MOCK_API=false
