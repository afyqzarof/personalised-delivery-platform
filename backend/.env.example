# Copy to .env for local development; values are loaded on startup.

# Port the API listens on. Defaults to 3000 when unset.
PORT=3000

# Allowed browser origin(s) for CORS, comma-separated.
# Leave unset to reflect the request origin (handy in local dev).
FRONTEND_ORIGIN=http://localhost:5173
